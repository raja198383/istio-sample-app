apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    alb.ingress.kubernetes.io/actions.ssl-redirect: '{"Type": "redirect", "RedirectConfig":
      { "Protocol": "HTTPS", "Port": "443", "StatusCode": "HTTP_301"}}'
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-west-2:963628282491:certificate/671f8883-9065-4a16-936f-34a23f465c7a
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS":443}]'
    alb.ingress.kubernetes.io/scheme: internal
    external-dns.alpha.kubernetes.io/hostname: cluster15.intellectproduct.com
  name: istio-frb-alb
  namespace: istio-system
spec:
  rules:
  - http:
      paths:      
      - backend:
          service:
            name: istio-ingressgateway
            port:
              number: 80
        path: /*
        
